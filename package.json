{
  "name": "slime",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "node -e \"require('fs').rmSync('.check',{recursive:true,force:true})\" && tsc --ignoreConfig src/game/checks.ts --rootDir src --outDir .check --module esnext --target es2022 --moduleResolution bundler --skipLibCheck && node .check/game/checks.js",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
		"vite": "^8.0.16",
    "typescript": "~6.0.3"
  }
}
