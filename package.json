{
  "name": "slime",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --ignoreConfig src/game/checks.ts --outDir .check --module esnext --target es2022 --moduleResolution bundler --skipLibCheck && node .check/checks.js",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
		"vite": "^8.0.16",
    "typescript": "~6.0.3"
  }
}
